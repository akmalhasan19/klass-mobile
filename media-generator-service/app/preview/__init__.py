"""Preview artifact handling for the Marp HTML pipeline.

This package provides helpers for persisting self-contained HTML previews
and generating signed download URLs that the Flutter client can load in
``flutter_inappwebview``.
"""
